### `py -m ensurepip --upgrade` (only if pip is not installed)
### `pip install -r requirements.txt`
### `.\Scripts\activate.bat` (if running from cmd, not pycharm)
After starting the server, wait for it to start (around 20s)
